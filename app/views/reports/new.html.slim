h1 New Report

= render 'shared/errors', resource: @report
= form_with model: @report do |f|
  = f.label :title
  = f.text_field :title

  = f.label :description
  = f.label :text_area

  = f.submit 'Create Report'